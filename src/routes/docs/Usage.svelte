<h2>Installation</h2>
<p>Use your preferred node package manager.</p>
<div class="flex flex-col [&>*]:w-fit gap-y-1">
	<code> npm i @arisbh/elaimant </code>
	<code>pnpm add @arisbh/elaimant </code>
	<code>yarn add @arisbh/elaimant</code>
</div>

<h2>Usage</h2>
<p>
	You can throw any element in Marqueeck, solo or grouped, a simple div or another Svelte component,
	or even just plain text for the sake of simplicity.
</p>
<p>
	Your element will automaticaly be repeated inside the component, auto-updating the number of
	needed elements to fill its parent.
</p>
<p>Import the <code>Marqueeck</code> component, and wrap your content with it.</p>
<!-- todo -->
<code>...code...</code>

<h2>Advanced configuration</h2>

<h3>Sticky elements</h3>
<p>You can use the reserved svelte:fragment to place a sticky element inside your component.</p>
<!-- todo -->
<code>...code...</code>

<h3>Separator</h3>
<p>
	You can use the reserved svelte:fragment to place a separator between your repeated component.
</p>
<!-- todo -->
<code>...code...</code>

<h2>Learn more</h2>
<p>Check the <code>Props</code> or <code>Events</code> tabs for even more informations.</p>
