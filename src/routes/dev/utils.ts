export function showNodeDetails(node: HTMLElement) {
    // console.log("Id of the element: " + (node.id == "" ? node.id : "none"))
    // console.log("TagName: " + node.tagName ?? "none")
    // console.log("Element classes: " + node.className)
    // console.log("Text:" + node.innerText)
    console.log(node)
}